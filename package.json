{
  "name": "sequent",
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "private": true,
  "scripts": {
    "format": "prettier --write packages/**/*.ts",
    "build": "ls packages | xargs -I% -- bun --cwd packages/% build",
    "check": "ls packages | xargs -I% -- bun --cwd packages/% check"
  },
  "workspaces": [
    "packages/*"
  ]
}
